defaults: base.yaml
logs_root: logs/fl
checkpoints_root: checkpoints/fl
outputs_root: outputs/fl

algorithm: FedBN
domain_adaptation: true      # BatchNorm params stay local per hospital/domain (src/models/fedbn.py)

global_epochs: 50
local_epochs: 3
